<ui:FluentWindow x:Class="WxlConverterUi.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Conversion log" Height="560" Width="720"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True">
    <Grid Margin="16,0,16,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Conversion log" Margin="-16,0,-16,0" />

        <TextBlock x:Name="SummaryText" Grid.Row="1" Margin="0,0,0,12" TextWrapping="Wrap" FontWeight="SemiBold" />

        <Border Grid.Row="2" BorderThickness="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                CornerRadius="4" Margin="0,0,0,12">
            <ListBox x:Name="LogList" FontFamily="Consolas" FontSize="12"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto" BorderThickness="0" Background="Transparent" />
        </Border>

        <ui:Button Grid.Row="3" Content="Close" HorizontalAlignment="Right" Click="Close_Click" />
    </Grid>
</ui:FluentWindow>
